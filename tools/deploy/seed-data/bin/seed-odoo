#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
JAR="$SCRIPT_DIR/../target/seed-data.jar"
if [[ ! -f "$JAR" ]]; then
    echo "seed-data.jar not built. Run: (cd $SCRIPT_DIR/.. && mvn -q package)" >&2
    exit 1
fi
exec java -jar "$JAR" odoo "$@"
