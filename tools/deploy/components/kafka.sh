# shellcheck shell=bash
# Component handler: kafka — stub
#
# When implementing: docker-compose up + setup-topics.sh on remote.

deploy_kafka() {
    echo "TODO: kafka deploy handler not implemented yet."
    exit 1
}
