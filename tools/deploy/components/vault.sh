# shellcheck shell=bash
# Component handler: vault (OpenBao) — stub
#
# When implementing: ship vault config, init/unseal handling.

deploy_vault() {
    echo "TODO: vault deploy handler not implemented yet."
    exit 1
}
