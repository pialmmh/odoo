# shellcheck shell=bash
# Component handler: kb (Kill Bill) — stub
#
# When implementing: ship catalog XML, restart Kill Bill service.

deploy_kb() {
    echo "TODO: kb (Kill Bill) deploy handler not implemented yet."
    exit 1
}
